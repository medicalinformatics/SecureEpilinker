# link me in build directory and start with
# tmuxp load test_aby.tmuxp.yaml
session_name: test_aby
windows:
- test_aby:
  panes:
    - shell_command:
      - alias test_aby="./test_aby"
      - test_aby
    - shell_command:
      - alias test_aby="./test_aby -S"
      - test_aby
  options_after:
    synchronize-panes: on

